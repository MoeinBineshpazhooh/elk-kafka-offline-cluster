# Each pipeline is isolated and mounted explicitly by Docker Compose.
# Add a new pipeline by creating its own configuration file and pipeline entry.

- pipeline.id: demo-kafka-to-elasticsearch
  path.config: "/usr/share/logstash/pipeline/demo-kafka-to-elasticsearch.conf"
  pipeline.workers: 2
  pipeline.batch.size: 125
  pipeline.batch.delay: 50
